# Parent repo has a top-level `.railwayignore` that ignores everything (`**`)
# and allowlists only a few directories/files.
#
# This repo is deployed as its own service, so we re-include everything here
# and then ignore only local build artifacts.

!**

.git/
.env
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/
.DS_Store
