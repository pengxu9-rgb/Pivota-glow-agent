{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "env PYTHONPATH=/app /opt/venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  }
}
